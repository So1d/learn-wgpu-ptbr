# Renderização Headless (Sem Janela)

Demonstração de renderização offscreen em segundo plano (sem criar uma janela do SO) salvando o resultado diretamente em um arquivo de imagem (PNG).

<AutoGithubLink/>
